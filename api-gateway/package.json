{
  "name": "@domislink/api-gateway",
  "version": "1.0.0",
  "description": "Cloudflare Workers API Gateway for DomisLink Empire",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.22.0"
  }
}
