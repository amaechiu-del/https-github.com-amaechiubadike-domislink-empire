# Cloudflare Workers configuration for DomisLink Empire
# This is a placeholder configuration - actual Workers are deployed from domislinker/
# Pages projects are configured via Cloudflare Dashboard, not wrangler.toml

name = "https-githubcomamaechiubadike-domislink-empire"
compatibility_date = "2024-01-01"

# Main entry point - a simple health check worker
main = "domislinker/workers/api.js"

[vars]
ENVIRONMENT = "production"